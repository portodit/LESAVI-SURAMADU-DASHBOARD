{
  "name": "lesavi-suramadu-dashboard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/telkom-am-dashboard run dev",
    "dev:api": "PORT=8080 pnpm --filter @workspace/api-server run dev",
    "db:push": "pnpm --filter @workspace/db run push"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "protobufjs"
    ]
  }
}